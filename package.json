{
    "name": "spacepathcalculator",
    "devDependencies": {
        "browserify": "latest"
    },
    "scripts": {
        "build": "tsc src/index --outDir 'build' \n browserify build/index.js -o build/index.js"
    }
}