{
    "name": "spacepathcalculator",
    "devDependencies": {
        "browserify": "latest"
    },
    "scripts": {
        "build": "tsc src/ts/index --outDir build/ --target  ESNext --removeComments true --module CommonJS && browserify build/index.js -o build/index.js && copyfiles -f src/index.html build"
    }
}
